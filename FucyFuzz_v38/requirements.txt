# FucyFuzz v14 — Python dependencies
# Install all: pip install -r requirements.txt

# ── GUI ──────────────────────────────────────────────────────────────────────
PyQt5>=5.15

# ── CAN bus ──────────────────────────────────────────────────────────────────
python-can>=4.0.0
python-isotp>=1.3.0

# ── Virtual CAN (Windows) ────────────────────────────────────────────────────
# python-can virtual bus works out-of-the-box, no extra packages needed.
# For PCAN USB hardware (optional): pip install 'python-can[pcan]'

# ── Optional: DBC file parsing ───────────────────────────────────────────────
# cantools>=39.0.0

# ── Optional: MF4 export ─────────────────────────────────────────────────────
# asammdf>=7.0

# NOTE: uptime, psutil, pyyaml, rclpy, pymodbus are NOT required.
# All vehicle/ROS2/hardware dependencies have been removed in v14.
